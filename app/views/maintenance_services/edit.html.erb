<% content_for :title, "Editando servicio de mantenimiento" %>

<div class="row justify-content-center">
  <div class="col-md-6">
    <h1 class="mb-4">Editando servicio de mantenimiento</h1>

    <%= render "form", maintenance_service: @maintenance_service %>

    <div class="mt-3 d-flex">
      <%= link_to "Mostrar este servicio de mantenimiento", car_maintenance_service_path(@car, @maintenance_service), class: 'btn btn-secondary me-2' %>
      <%= link_to "Volver a los servicios de mantenimiento", car_maintenance_services_path(@car), class: 'btn btn-secondary' %>
    </div>
  </div>
</div>
